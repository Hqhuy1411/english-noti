# Local development only. Copy to .env and fill in -- .env is gitignored.
# On Lambda the token comes from SSM Parameter Store instead, never from here.

# From @BotFather on Telegram (/newbot)
TELEGRAM_BOT_TOKEN=

# Run `node services/notifier/scripts/get-chat-id.mjs` to discover this
TELEGRAM_CHAT_ID=
